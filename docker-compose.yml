version: "3.3"

services:
 server:
   build:
    context: .
    dockerfile: docker/PythonDockerfile
   ports:
    - "4000:4000"
   networks:
    - "net"

 mongo:
  image: mongo
  ports:
    - "27017:27017"
  networks:
   - "net"

networks:
  net:
    driver: bridge
